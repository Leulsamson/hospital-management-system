import React from "react";
import Link from "next/link";

export default function Page() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-6xl items-center px-4 py-8 md:px-8">
      <section className="card-soft fade-slide-up w-full rounded-3xl p-8 md:p-12">
        <p className="text-xs font-semibold tracking-[0.24em] text-[var(--brand)]">HOSPITAL MANAGEMENT SYSTEM</p>
        <h1 className="mt-4 max-w-3xl font-[var(--font-heading)] text-4xl leading-tight md:text-6xl">
          Clinical operations with modern architecture and secure workflows.
        </h1>
        <p className="mt-6 max-w-2xl text-base text-soft md:text-lg">
          Your foundation is production-grade: PostgreSQL, Prisma 7, session auth, and role guards are active.
          Continue to the dashboard to start building feature modules.
        </p>

        <div className="mt-8 flex flex-wrap gap-3">
          <Link
            href="/login"
            className="rounded-full bg-[var(--brand)] px-5 py-3 text-sm font-semibold text-white transition hover:brightness-95"
          >
            Sign in
          </Link>
          <Link
            href="/dashboard"
            className="rounded-full border border-[var(--line)] bg-white px-5 py-3 text-sm font-semibold text-[var(--ink)] transition hover:border-[var(--brand)]"
          >
            View dashboard
          </Link>
        </div>
      </section>
    </main>
  );
}
